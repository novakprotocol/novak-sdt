# Proofs

## What is proved now

- `sdt` installs and runs on the real control host
- `sdt doctor` works
- `sdt baseline --report-only` works
- the repo report generator emits Markdown and HTML
- the template validator exists and passes
- a clean birth proof exists from template to first independent commit

## Why this matters

This means SDT is not only theory or repo doctrine.
It has already crossed into working software and working operator flow.

## Public proof surfaces

- `reports/novak-sdt-report.html`
- `reports/template-report.html`

## Strongest current claim

SDT can birth a fresh repo from the template, validate the floor, prove the floor with the CLI, generate reports, and commit the result with clean independent Git history.
